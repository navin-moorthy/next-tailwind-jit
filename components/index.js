import * as React from 'react';

import { Button as RenderlesskitButton } from '@renderlesskit/react-tailwind';

export const Button = (props) => {
  return <RenderlesskitButton {...props}>Continues</RenderlesskitButton>;
};

export default Button;